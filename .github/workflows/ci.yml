name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  smoke:
    name: Smoke runner
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner starts
        run: echo "runner-started"
