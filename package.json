{
  "name": "social-challenge-arena",
  "version": "0.3.0",
  "private": true,
  "description": "Mobile-first social challenge MVP for one-link friend competition.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
