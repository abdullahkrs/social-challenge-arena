{
  "name": "social-challenge-arena",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile-first social challenge MVP.",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "engines": {
    "node": ">=20"
  }
}
